name = "difafalkh"
compatibility_date = "2024-03-20"

# This tells Cloudflare Workers to serve the static files from the dist folder
assets = { directory = "dist", not_found_handling = "single-page-application" }
